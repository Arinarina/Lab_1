#pragma once

#include <iostream> 
#include <stdio.h> 
#include <string> 
#include "worker.h" 

using namespace std;

void copy(char* a, char* b);

long int dlina(char* a);

bool chislo(char p);

bool test_number(char* str);

bool test_stroka(char* str);

